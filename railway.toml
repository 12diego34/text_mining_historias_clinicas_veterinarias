[build]
builder = "dockerfile"

[deploy]
startCommand = "python manage.py migrate && gunicorn fichas_vet.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
